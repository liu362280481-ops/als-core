# 绝对物理法则：ALS V2 创世约束
1. 核心底座不可侵犯：绝对禁止修改 `als-core` 中的核心算子（如 `diffusion_engine.sv`, `reaction_engine.sv` 等），它们是验证过的物理真理。
2. 数据格式：全系统强制使用 Q8.8 定点数（16-bit，精度 1/256），禁止使用任何浮点逻辑。
3. 时序架构：强制遵守 AXI-Stream 流形协议与 Line Buffer（行缓冲）架构，禁止使用软件轮询思维。
4. 行为规范：每次修改不得超过 3 个文件。在写代码前，必须先输出实现计划。